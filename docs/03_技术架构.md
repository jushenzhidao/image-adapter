# OpenAI Images 适配微服务 — 技术架构

## 1. 技术选型

| 层级           | 组件       | 选型                          | 理由                                       |
| ------------ | -------- | --------------------------- | ---------------------------------------- |
| **Web 框架**   | HTTP 服务  | `Starlette` (ASGI)          | 轻量、高性能、原生支持 StreamingResponse            |
| **ASGI 服务器** | 生产服务器    | `uvicorn` + `gunicorn`      | 多 Worker 进程绕过 GIL，单 Worker 内 asyncio 高并发 |
| **HTTP 客户端** | 上游调用     | `aiohttp`                   | 异步、连接池、流式下载、超时精细控制                       |
| **图片处理**     | 编解码/格式转换 | `Pillow` (PIL)              | 成熟稳定，支持常见格式转换和缩略图                        |
| **对象存储**     | 临时图片存储   | `MinIO` (S3 兼容)             | 轻量、本地部署、预签名 URL、自动过期策略                   |
| **缓存/状态**    | 分布式状态    | `Redis` (aioredis)          | 异步客户端、支持 Hash/Expire、分布式锁                |
| **可观测**      | 全链路追踪    | `Logfire` (Pydantic)        | Python 原生、OpenTelemetry 标准、自动 FastAPI 埋点 |
| **配置管理**     | 配置加载     | `Pydantic Settings`         | 类型安全、环境变量自动映射、验证                         |
| **脚本热重载**    | 文件监听     | `watchdog`                  | 跨平台、低延迟、资源占用低                            |
| **序列化**      | JSON 处理  | 原生 `json` + `orjson` (可选)   | 标准库足够，orjson 用于极致性能场景                    |
| **容器化**      | 部署       | `Docker` + `Docker Compose` | 单文件编排、开发/生产一致、脚本卷挂载热更新                   |

---


## 2. 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              客户端层                                       │
│  OpenAI Python SDK / HTTP Client / LangChain / 第三方应用                    │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼ HTTP/HTTPS
┌─────────────────────────────────────────────────────────────────────────────┐
│                           New API (现有网关层)                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐│
│  │  统一鉴权    │  │  配额计费    │  │  智能路由    │  │     模型映射        ││
│  │  API Key    │  │  Rate Limit │  │  LoadBalance │  │  Model Alias        ││
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────────┘│
│                                    │                                        │
│                    同构上游 ────────┼──────► 直接转发 (OpenAI/Azure/Claude)  │
│                    异构上游 ────────┼──────► 转发到 Adapter                   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼ HTTP/HTTPS (OpenAI 兼容协议)
┌─────────────────────────────────────────────────────────────────────────────┐
│                    OpenAI Adapter (纯 Python 服务)                           │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  HTTP 接入层 (Starlette ASGI)                                      │   │
│  │  POST /v1/chat/completions                                        │   │
│  │  POST /v1/responses                                               │   │
│  │  POST /v1/images                                                  │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │   │
│  │  │ 鉴权中间件 │ │ 限流中间件 │ │ 日志中间件 │ │ CORS中间件 │           │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  核心调度器 (asyncio)                                               │   │
│  │  1. 解析 model → Registry 查 upstream + endpoint                   │   │
│  │  2. 创建 AdapterContext (注入 logfire span + infra handles)        │   │
│  │  3. Engine 加载脚本 → Sandbox 执行 → 返回结果                      │   │
│  │  4. 上游 HTTP 调用 (aiohttp)                                       │   │
│  │  5. 响应转换 → 返回客户端                                         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                    ┌───────────────┼───────────────┐                       │
│                    ▼               ▼               ▼                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  基础设施句柄 (通过 ctx 注入脚本，禁止直接 import)                  │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐             │   │
│  │  │ aiohttp   │ │ aioredis │ │ minio    │ │ logfire  │             │   │
│  │  │ HTTP客户端 │ │ 状态缓存  │ │ 对象存储  │ │ 可观测   │             │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                    ┌───────────────┼───────────────┐                       │
│                    ▼               ▼               ▼                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  脚本沙箱层 (script_store/)                                         │   │
│  │  volcengine_ark/images@v1.py                                        │   │
│  │  (X-Script-Ref: volcengine_ark/images@v1)                           │   │
│  │  (importlib 动态加载 + AST 安全扫描 + watchdog 热重载)            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
            ┌──────────┐    ┌──────────┐    ┌──────────┐
            │ 上游 A   │    │ 上游 B   │    │ 上游 C   │
            │ 国产文生图│    │ 国产多模态│    │ SD / MJ  │
            └──────────┘    └──────────┘    └──────────┘
```

---

## 3. 组件详细设计

### 3.1 HTTP 接入层 (`adapter/main.py`)

```python
# 端点注册
app = Starlette(routes=[
    Route("/v1/chat/completions", chat_completions, methods=["POST"]),
    Route("/v1/responses", responses, methods=["POST"]),
    Route("/v1/images", images, methods=["POST"]),
    Route("/health", health, methods=["GET"]),
])

# 中间件栈
app.add_middleware(AuthenticationMiddleware)   # 内部 Token 校验
app.add_middleware(RateLimitMiddleware)        # 基于 Redis 的滑动窗口限流
app.add_middleware(LoggingMiddleware)          # 请求/响应日志
app.add_middleware(CORSMiddleware)             # 跨域支持
```

**流式输出支持**：

- 非流式：直接返回 JSON Response
- 流式（SSE）：使用 `StreamingResponse`，yield `data: {...}\n\n` 格式
- 上游非流式但客户端要求 SSE：Gateway 层将完整响应拆分为多个 chunk 推送


### 3.2 核心调度器 (`adapter/router.py`)

**路由决策流程**：

```
接收请求
    │
    ├──► 解析 body → 提取 model 字段
    │
    ├──► Registry 查询 → upstream_name + endpoint
    │     (model "vendor-a-text2img" → upstream: "upstream_a", endpoint: "images")
    │
    ├──► 创建 AdapterContext
    │     • request_id = uuid4()
    │     • deadline = now + 总预算（单级 60s / 级联 STAGE_BUDGET_DEFAULT）
    │     • logfire span = logfire.span("adapt", endpoint=..., upstream_url=...)
    │     • http = aiohttp.ClientSession(pool_size=20)
    │     • cache = aioredis.Redis()
    │     • storage = MinioClient()
    │     • image = ctx.image      # 受限 Pillow 门面（v1.1）
    │
    ├──► Engine 加载脚本
    │     • 缓存命中 → 直接返回 module
    │     • 缓存未命中 → importlib 加载 → AST 扫描 → 存入缓存
    │
    ├──► 读取模块级 STAGES；未声明则视为单级 [""]（行为同现状）
    │
    └──► for stage in STAGES:            # 级联循环，见 §3.8
          ├─ transform(ctx, body, f"request:{stage}")   → 上游请求体 + ctx.emit()
          ├─ aiohttp 发送请求到上游（span: upstream_call，超时取 min(级超时, ctx.remaining)）
          ├─ 同步响应 → 直接处理 / 异步 Job ID → AsyncStateMachine 轮询
          ├─ transform(ctx, resp, f"response:{stage}")  → 存入 ctx.stage[stage]
          └─ 失败且 stage ∈ STAGE_FALLBACK → 用上一级产物降级，继续
         末级产物即 OpenAI 响应 → 返回客户端
```


### 3.3 脚本执行引擎 (`adapter/engine.py`)

```python
class ScriptEngine:
    def __init__(self, script_path: str):
        self.script_path = script_path
        self.modules: dict[str, ModuleType] = {}  # 模块缓存
        self.lock = asyncio.Lock()

    async def load(self, upstream: str, endpoint: str) -> ModuleType:
        key = f"{upstream}.{endpoint}"
        if key not in self.modules:
            async with self.lock:
                if key not in self.modules:  # 双重检查
                    module = await self._load_module(upstream, endpoint)
                    self.modules[key] = module
        return self.modules[key]

    async def _load_module(self, upstream: str, endpoint: str) -> ModuleType:
        file_path = f"{self.script_path}/{upstream}/{endpoint}.py"
        # 1. 读取源码
        source = await aiofiles.open(file_path).read()
        # 2. AST 安全扫描
        tree = ast.parse(source)
        SandboxScanner.scan(tree)  # 禁止 exec/eval/subprocess
        # 3. 编译加载
        spec = importlib.util.spec_from_file_location(f"{upstream}.{endpoint}", file_path)
        module = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(module)
        return module

    async def transform(self, ctx, payload: dict, ref: str, phase: str) -> dict:
        module = await self.load(ref)
        with ctx.logfire.span("script_transform", script=ref, phase=phase):
            return await asyncio.wait_for(
                module.transform(ctx, payload, phase),
                timeout=ctx.settings.script_timeout
            )
```

**缓存与失效**：

- 缓存 key 是脚本源码的 `sha256`，不是路径或 `mtime`
- 脚本一改，摘要自然变化，命中新 key 并重新执行 AST 安全扫描
- 因此**不需要** `watchdog` 之类的文件监听，脚本目录只读挂载也不会失效

#### 3.3.1 分层超时模型（v1.1，取代单一 30s 墙钟）

> 以实际代码为准：超时项在 `adapter/settings.py`，阶段调用在 `adapter/executor.py::_call_phase`。

现状 `_call_phase` 对每个 phase 施加 `settings.script_timeout`(30s)。该值约束的是**脚本函数自身**的执行，  
而等待上游的时间落在 `_do_upstream` 的 `upstream_timeout`(60s) 上——两者本就分离，所以 30s 不必放宽。  
级联真正缺的是**跨级的总预算**：

| 层级    | 配置项                                                | 默认            | 适用                                       | 超时行为                               |
| ----- | -------------------------------------------------- | ------------- | ---------------------------------------- | ---------------------------------- |
| 脚本函数  | `script_timeout`                                   | 30s           | 每个 `transform()` 调用（含 `request:{stage}`） | 504，`code=script_timeout`          |
| 单次上游  | `upstream_timeout`，可被 `ctx.emit(timeout=)` 覆盖      | 60s           | 每级的上游 HTTP 调用                            | 504，`code=upstream_timeout`        |
| 级联总预算 | `pipeline_max_timeout` / `X-Stage-Timeout: total=` | 300s（上限 600s） | 整条级联                                     | 504，`code=pipeline_timeout` + 当前级名 |

关键规则（BR-011）：单次上游的实际超时取 `min(plan.timeout or upstream_timeout, budget.remaining)`，  
故各级超时**不会相加溢出总预算**。未声明 `STAGES` 的脚本行为完全不变。

### 3.4 安全沙箱 (`adapter/sandbox.py`)

```python
class SandboxScanner(ast.NodeVisitor):
    BLACKLIST = {
        'Exec', 'Eval', 'ExecBody', 'Interactive',
        'Subscript', 'Await'  # 视安全策略调整
    }

    def visit_Import(self, node):
        for alias in node.names:
            if alias.name not in ALLOWED_STDLIB:
                raise SecurityError(f"Forbidden import: {alias.name}")
        self.generic_visit(node)

    def visit_ImportFrom(self, node):
        if node.module not in ALLOWED_STDLIB:
            raise SecurityError(f"Forbidden import from: {node.module}")
        self.generic_visit(node)

    def visit_Call(self, node):
        if isinstance(node.func, ast.Name) and node.func.id in ('exec', 'eval'):
            raise SecurityError(f"Forbidden call: {node.func.id}")
        self.generic_visit(node)
```

**允许的标准库清单**：  
`json`, `re`, `base64`, `datetime`, `time`, `math`, `random`, `string`, `typing`, `collections`, `itertools`, `functools`, `hashlib`, `uuid`, `urllib.parse`


### 3.5 上下文对象 (`adapter/context.py`)

脚本与基础设施交互的唯一通道：

```python
class AdapterContext:
    def __init__(self, request_id: str, model: str, upstream: str, 
                 upstream_config: dict, settings: Settings):
        self.request_id = request_id
        self.model = model
        self.upstream_name = upstream
        self.upstream_config = upstream_config
        self.settings = settings

        # 基础设施句柄（懒加载）
        self._http: aiohttp.ClientSession | None = None
        self._cache: aioredis.Redis | None = None
        self._storage: Minio | None = None

        # Logfire span（由 Gateway 层注入）
        self.logfire: logfire.Logfire = logfire.span("adapter_context")

    @property
    def http(self) -> aiohttp.ClientSession:
        if self._http is None:
            self._http = aiohttp.ClientSession(
                timeout=aiohttp.ClientTimeout(total=self.upstream_config.get("timeout", 60)),
                connector=aiohttp.TCPConnector(limit=20)
            )
        return self._http

    @property
    def cache(self) -> aioredis.Redis:
        if self._cache is None:
            self._cache = aioredis.from_url(self.settings.redis_url)
        return self._cache

    @property
    def storage(self) -> Minio:
        if self._storage is None:
            self._storage = Minio(
                self.settings.minio_endpoint,
                access_key=self.settings.minio_access_key,
                secret_key=self.settings.minio_secret_key,
                secure=False
            )
        return self._storage

    async def download_image(self, url: str) -> bytes:
        with self.logfire.span("download_image", url=url[:50]):
            # 缓存检查
            cache_key = f"img_cache:{hashlib.md5(url.encode()).hexdigest()}"
            cached = await self.cache.get(cache_key)
            if cached:
                return base64.b64decode(cached)

            # 下载
            async with self.http.get(url) as resp:
                data = await resp.read()

            # 缓存 5 分钟
            await self.cache.set(cache_key, base64.b64encode(data).decode(), ex=300)
            return data

    async def upload_temp_image(self, data: bytes, ext: str = "png") -> str:
        with self.logfire.span("upload_temp_image", size_bytes=len(data)):
            key = f"temp/{self.request_id}/{uuid.uuid4()}.{ext}"
            self.storage.put_object(self.settings.minio_bucket, key, io.BytesIO(data), len(data))
            return self.storage.presigned_get_object(
                self.settings.minio_bucket, key, expires=timedelta(hours=1)
            )

    def encode_b64(self, data: bytes, mime: str = "image/png") -> str:
        return f"data:{mime};base64,{base64.b64encode(data).decode()}"
```

#### 3.5.1 级联编排增强能力（v1.1）

> 以实际代码 `adapter/context.py` 为准。现有 ctx 已提供 `http`/`cache`/`storage`/`emit`/  
> `download_image`/`upload_temp_image`/`image_bytes`/`image_b64`/`image_data_uri`/`image_url`/  
> `encode_b64`/`decode_b64`/`data_uri`/`sniff_mime`/`sleep`，级联只需补三项。

| 新增                               | 用途                                                          | 备注                            |
| -------------------------------- | ----------------------------------------------------------- | ----------------------------- |
| `ctx.image`                      | 前/后处理算子（resize/crop/pad/convert/compress/info/compose_mask） | Pillow 已在依赖，见 §3.8.7；沙箱白名单不放宽 |
| `ctx.stage`                      | 只读 dict，读取已完成级的产物（`ctx.stage["generate"]["image"]`）         | 引擎在每级 `response:{stage}` 后写入  |
| `ctx.stage_urls`                 | 各级上游 URL（来自 `X-Stage-Urls`，已过 SSRF 校验）                      | 脚本用它 `ctx.emit(url=...)`      |
| `ctx.deadline` / `ctx.remaining` | 剩余总预算（秒），脚本可据此裁剪重试或轮询                                       | 由 §3.8.4 的 `Budget` 投影，只读     |
| `ctx.SKIP`                       | 哨兵，`request:{stage}` 返回它则整级跳过（§3.8.8）                       | 模块级单例，脚本不可构造                  |

**无需新增** `ctx.upstream(name)`：真实架构里上游 URL 本就由 channel 头提供、  
凭证由引擎按 `X-Auth-Emit` 注入，脚本用 `ctx.emit(url=...)` 指定目标即可，  
凭证从不进入脚本可见域。这比引入注册表式的跨上游客户端更贴合现有模型。

**无需新增** `ctx.step()`：级 Span 由引擎在 `execute_staged` 中统一生成（§3.8.3），  
脚本不必也不应自己开 Span。


### 3.6 上游注册表 (`adapter/registry.py`)

YAML 格式配置，支持热刷新：

```yaml
upstreams:
  upstream_a:
    base_url: "https://vendor-a.com/api/v2"
    timeout: 60
    async_mode: false
    auth_type: "header"  # header / ak_sk / none
    auth_config:
      header_name: "X-API-Key"
      env_key: "UPSTREAM_A_API_KEY"
    models:
      - vendor-a-text2img
      - vendor-a-vision
    endpoints:
      - chat
      - images

  upstream_b:
    base_url: "https://vendor-b.com/v1"
    timeout: 120
    async_mode: true
    poll_interval: 2
    poll_timeout: 120
    auth_type: "ak_sk"
    auth_config:
      ak_env: "UPSTREAM_B_AK"
      sk_env: "UPSTREAM_B_SK"
      sign_version: "v3"
    models:
      - vendor-b-multimodal
    endpoints:STAGES
      - chat
      - response

  upstream_sr:                        # 超分上游（级联后处理，v1.1）
    base_url: "https://vendor-sr.com/v1"
    timeout: 120
    async_mode: false
    auth_type: "header"
    auth_config:
      header_name: "Authorization"
      env_key: "UPSTREAM_SR_API_KEY"
    endpoints:
      - images

```

> 级联（v1.1）**不引入注册表配置**。实际架构里通道由请求头声明，级联同样如此：  
> 脚本用模块级 `STAGES` 声明级次，控制面用 `X-Stages` / `X-Stage-Urls` / `X-Stage-Timeout`  
> 覆盖，详见 §3.8.2 与 §3.8.6。


### 3.7 异步状态机 (`adapter/async_state.py`)

处理上游异步 Job 模式：

```python
class AsyncStateMachine:
    def __init__(self, ctx: AdapterContext, script: ModuleType):
        self.ctx = ctx
        self.script = script

    async def run(self, job_id: str) -> dict:
        with self.ctx.logfire.span("async_job_poll", job_id=job_id):
            poll_interval = self.ctx.upstream_config.get("poll_interval", 2)
            poll_timeout = self.ctx.upstream_config.get("poll_timeout", 120)

            start = time.monotonic()
            while time.monotonic() - start < poll_timeout:
                # 分布式锁：防止多 Worker 同时轮询同一 Job
                lock_key = f"job_lock:{job_id}"
                locked = await self.ctx.cache.set(lock_key, "1", nx=True, ex=poll_interval + 1)

                if locked:
                    try:
                        result = await self.script.handle_async_job(self.ctx, job_id)
                        if result.get("status") == "completed":
                            await self.ctx.cache.delete(lock_key)
                            return result
                        if result.get("status") == "failed":
                            raise UpstreamError(result.get("error", "Job failed"))
                    finally:
                        await self.ctx.cache.delete(lock_key)

                await asyncio.sleep(poll_interval)

            raise TimeoutError(f"Async job {job_id} poll timeout after {poll_timeout}s")
```


### 3.8 级联编排 (`adapter/stages.py`，v1.1)

> 本节以**实际代码结构**为准（channel 头驱动 + `transform(ctx, payload, phase)` 单入口），  
> 而非 §3.2/§3.6 遗留的 registry/YAML 描述。
>
>

#### 3.8.1 为什么现有引擎不够

`executor.execute()` 的链路是固定的：`auth → request → 【1 次上游调用】→ [poll] → response`。  
上游调用由引擎独占，脚本只能描述**一次**请求。级联需要 N 次调用，且第 2 次的入参依赖第 1 次的产物，  
现有阶段机无法表达。

#### 3.8.2 方案：新增 `stages` 阶段，引擎驱动多轮

不改变「脚本不碰 socket、引擎不懂厂商语义」这一根本约定，只把「一轮」扩展为「按脚本声明的 N 轮」。

脚本通过模块级 `STAGES` 声明级联，引擎逐级驱动：

```python
# script_store/vendor_x/hd@v1.py
PHASES = ["request", "response"]
STAGES = ["generate", "upscale"]        # 声明级联，引擎按序执行

async def transform(ctx, payload, phase):
    # 前处理：纯本地计算，不产生上游调用
    if phase == "request:generate":
        ctx.emit(url=ctx.options["generate_url"])
        return {"prompt": payload["prompt"], "size": "2k"}

    if phase == "response:generate":
        # 产物存入 ctx.stage，供下一级引用
        return {"image": payload["data"][0]["url"]}

    if phase == "request:upscale":
        ctx.emit(url=ctx.options["upscale_url"], timeout=120)
        return {"image": ctx.stage["generate"]["image"], "scale": 2}

    if phase == "response:upscale":
        return {"created": 0, "data": [{"url": payload["output"]}]}
```

阶段命名规则：`{phase}:{stage}`。未声明 `STAGES` 的脚本，phase 名不带后缀，  
走原有单轮链路——**既有脚本与既有测试零改动**（AC-25）。

#### 3.8.3 引擎侧实现

```python
async def execute_staged(ctx, script, channel, settings, client_payload):
    """按 STAGES 逐级驱动；每级复用既有的单轮逻辑。"""
    budget = Budget(total=min(channel.stage_spec.timeout, settings.pipeline_max_timeout))
    ctx.stage = {}                       # 级间产物，脚本只读引用

    payload = client_payload
    for name in script.stages:
        with logfire.span("stage", name=name, remaining_s=budget.remaining) as span:
            ctx.plan = RequestPlan()
            body = await _call_phase(script, ctx, payload, f"request:{name}",
                                     settings.script_timeout)
            if body is SKIP:                  # AC-31：整级跳过，不发上游调用
                span.set_attribute("skipped", True)
                continue
            try:
                reply = await _do_upstream(ctx, channel, settings, body,
                                           budget=budget)
                if channel.async_spec.enabled:
                    reply = await _run_poll_loop(script, ctx, channel, settings, reply)
                out = await _call_phase(script, ctx, reply.payload,
                                        f"response:{name}", settings.script_timeout)
            except AdapterError as exc:
                if not _fallback_ok(script, name, ctx.stage):
                    raise
                span.set_attribute("degraded", True)
                logfire.warn("stage_degraded", stage=name, reason=str(exc))
                break                     # 降级：返回上一级产物
            ctx.stage[name] = out
            payload = out
    return _final_payload(ctx.stage)
```

关键点：

- **复用而非重写**：`_do_upstream` / `_run_poll_loop` / `_call_phase` 全部沿用，级联只是外层循环。
- **每级独立的 `ctx.plan`**：`emit()` 的 URL/method/timeout 覆盖按级隔离，不串味。
- **异步 Job 正交**：任一级都可以是 job 型上游，`poll_*` 阶段在该级内部完成。

#### 3.8.4 共享预算（取代单级超时相加）

```python
@dataclass
class Budget:
    total: float
    started: float = field(default_factory=time.monotonic)

    @property
    def remaining(self) -> float:
        return max(0.0, self.total - (time.monotonic() - self.started))
```

`_do_upstream` 的实际超时取 `min(plan.timeout or upstream_timeout, budget.remaining)`；  
`remaining <= 0` 直接抛 `PipelineTimeoutError`。这样各级超时**不会相加溢出总预算**（BR-011）。

#### 3.8.5 降级

`STAGE_FALLBACK = ["upscale"]` 声明哪些级可降级。该级失败且 `ctx.stage` 已有可用产物时，  
中断级联、返回上一级结果，响应头置 `X-Adapter-Degraded: upscale` 与  
`X-Adapter-Degraded-Reason: <error code>`，HTTP 仍为 200（BR-013）。  
后处理（超分/转码）适用；**首级不可降级**（无上一级产物可退，`STAGE_FALLBACK` 含首级时  
脚本加载期即报错）。

降级产物需要重新塑形：中间产物是级间交接形状，不是客户端响应。脚本在 `PHASES` 里声明  
`degraded` 后，引擎会以该 phase 回调一次，把手上的产物转成客户端 body（见 §3.8.10）。

#### 3.8.6 通道声明

级联同样由控制面通过 header 声明，与现有 channel 模型一致：

```
X-Stages:         generate,upscale                     # 覆盖脚本 STAGES（可选）
X-Stage-Urls:     generate=https://a.com/t2i,upscale=https://b.com/sr
X-Stage-Timeout:  total=300
```

`X-Stage-Urls` 的每个 URL 同样过 `check_url()` 的 SSRF 校验；脚本经 `ctx.options` /  
`ctx.stage_urls` 读取，凭证仍由引擎按 `X-Auth-Emit` 注入，不进脚本可见域（BR-014）。

#### 3.8.7 `ctx.image`：前处理算子

Pillow 已在 requirements（`pillow==12.3.0`）但未对脚本暴露。实现为受限门面  
（`adapter/utils/imageops.py`，bytes 进 bytes 出，无文件路径参数）：

```python
class ImageOps:
    """CPU 密集操作一律走 asyncio.to_thread，不在事件循环里直接调 PIL。"""
    async def info(self, data) -> dict: ...                 # 宽高/格式/模式
    async def resize(self, data, width, height,
                     keep_ratio=True, fmt=None) -> bytes: ...
    async def convert(self, data, fmt, quality=None) -> bytes: ...
    async def to_data_url(self, data) -> str: ...
```

沙箱白名单**不放宽**——脚本仍不能 `import PIL`，只能 `await ctx.image.resize(...)`。

防护（BR-012）：

- 解码前设 `Image.MAX_IMAGE_PIXELS` 并按 header 声明的宽高校验 50 MP 上限  
  （`settings.max_image_pixels`），**在 `load()` 分配位图之前**拦截解压炸弹；  
  Pillow 自身在 2× 上限处抛 `DecompressionBombError`，已归一为同一个  
  `code=image_too_large`。
- 输出复用 `settings.max_asset_bytes`（20 MB）上限。
- 输入/输出格式限定 PNG / JPEG / WEBP / GIF；JPEG 无 alpha 通道，RGBA 源自动展平。

> 设计稿列出的 `crop` / `pad` / `compress` / `compose_mask` 本次未实现——`compress`  
> 的语义已由 `convert(fmt, quality=)` 覆盖，其余三个在当前级联场景（缩放 + 转码）  
> 无调用方，按 YAGNI 暂缓，待有真实脚本需求再加。

#### 3.8.8 整级跳过（`ctx.SKIP`）

同一脚本要同时服务文生图与图生图时，`preprocess` 级在无入图的请求里无事可做。  
`request:{stage}` 返回哨兵 `ctx.SKIP` 即整级跳过——不发上游调用、不写 `ctx.stage`、  
不消耗预算，直接进入下一级（AC-31）：

```python
if phase == "request:preprocess":
    if not payload.get("image"):
        return ctx.SKIP                  # 文生图：无入图，跳过前处理
    return {"image": await ctx.image.resize(..., 1024, 1024)}
```

引擎侧在 `_call_phase` 返回后判定 `body is ctx.SKIP` 则 `continue`，  
Span 标 `skipped=True`。哨兵是模块级单例，不可被脚本构造，避免与合法 payload 混淆。

#### 3.8.9 批量放大控制

`n > 1` 时上游调用量为 `n × len(STAGES)`。约束（BR-015）：并发上限 `settings.stage_concurrency`（默认 3）；  
限流按实际上游调用次数而非请求数计费；`adapter_stage_upstream_calls_total` 独立暴露以便容量核算。

#### 3.8.10 实现落地时的设计调整（vs 设计稿）

实现过程中与 §3.8.2~3.8.8 设计稿有几处有意偏离，均为了简化或补足语义：

1. **阶段名走 `request:{stage}` / `response:{stage}`，`degraded` 单独成 phase**。  
   级联中某级失败降级时，`ctx.stage` 里拿到的中间产物是**级间交接形状**（如  
   `{"image": ...}`），不是合法的客户端响应（应为 `{"created":..,"data":[...]}`）。  
   因此降级分支若直接用上一级产物会返回一个残缺 body。新增 `degraded` phase（须在  
   `PHASES` 里显式声明），由脚本把中间产物重新塑形为客户端响应。
2. **预算检查点放在 `_do_upstream` 入口**，超时也归并到该函数统一分类。未声明 `STAGES` 时  
   `budget=None`、`stage=None`，行为与现状完全一致。
3. **`ctx.SKIP` 的判定用 `body is ctx.SKIP`**，哨兵是模块级单例 `_Skip`，不可被脚本构造，  
   `bool(ctx.SKIP)` 恒为 `False`——防止脚本把 skip 误当空 body 处理。
4. **`X-Stage-Urls` 的每个 URL 同样走 `check_url()`**，且要求 URL 键是 `X-Stages` 键的子集  
   （拼写错误的级名会在解析期被拒绝，而非静默路由到通道 URL）。
5. **stage 数量双上限**：脚本侧 `STAGES` 声明上限 8（`MAX_DECLARED_STAGES`），  
   控制面 `X-Stages` 覆盖上限 `settings.stage_max_count`（默认 8）。
6. **`context.py` 已超 300 行**（476），是历史遗留，本次未重构；其余涉及文件均在规范内。

---

## 4. 数据流时序图

### 4.1 标准异构转换（同步上游）

```
Client          Gateway          Engine          Script          Upstream
  │               │               │               │               │
  │── POST /v1/images ─────────►│               │               │
  │  model=vendor-a-text2img    │               │               │
  │               │               │               │               │
  │               │── ① 解析 model ─────────────►│               │
  │               │               │               │               │
  │               │◄── ② 返回 upstream_a/images ─│               │
  │               │               │               │               │
  │               │── ③ load_script() ─────────►│               │
  │               │               │               │               │
  │               │◄── ④ 返回 module ────────────│               │
  │               │               │               │               │
  │               │── ⑤ create_ctx() ───────────►│               │
  │               │               │               │               │
  │               │── ⑥ transform_request() ───►│               │
  │               │               │               │               │
  │               │◄── ⑦ 上游请求体 ─────────────│               │
  │               │               │               │               │
  │               │── ⑧ HTTP POST ─────────────────────────────►│
  │               │               │               │               │
  │               │◄── ⑨ 上游响应 ──────────────────────────────│
  │               │               │               │               │
  │               │── ⑩ transform_response() ──►│               │
  │               │               │               │               │
  │               │◄── ⑪ OpenAI 格式响应 ───────│               │
  │               │               │               │               │
  │◄── ⑫ 返回客户端 ────────────│               │               │
  │               │               │               │               │
```

### 4.2 异步上游（Job 模式）

```
Client          Gateway          AsyncStateMachine    Script          Upstream
  │               │               │                   │               │
  │── POST /v1/images ─────────►│                   │               │
  │               │               │                   │               │
  │               │── ① transform_request() ───────────────────────►│
  │               │               │                   │               │
  │               │◄── ② 返回 Job ID ───────────────────────────────│
  │               │               │                   │               │
  │               │── ③ AsyncStateMachine.run(job_id) ─────────────►│
  │               │               │                   │               │
  │               │               │── ④ 循环轮询 ──────────────────►│
  │               │               │   (interval=2s, timeout=120s)   │
  │               │               │                   │               │
  │               │               │◄── ⑤ status=running ───────────│
  │               │               │   sleep(2s)                     │
  │               │               │                   │               │
  │               │               │── ⑥ 再次轮询 ──────────────────►│
  │               │               │                   │               │
  │               │               │◄── ⑦ status=completed + result ─│
  │               │               │                   │               │
  │               │◄── ⑧ 最终结果 ──────────────────────────────────│
  │               │               │                   │               │
  │               │── ⑨ transform_response() ─────────────────────►│
  │               │               │                   │               │
  │◄── ⑩ 返回 OpenAI 格式 ──────│                   │               │
  │               │               │                   │               │
```

### 4.3 流式桥接（上游非流式 → 客户端 SSE）

```
Client          Gateway          Script          Upstream
  │               │               │               │
  │── POST /v1/chat ───────────►│               │
  │  stream=true                │               │               │
  │               │               │               │               │
  │               │── ① 完整请求到上游 ─────────────────────────►│
  │               │               │               │               │
  │               │◄── ② 完整响应 ───────────────────────────────│
  │               │               │               │
  │               │── ③ transform_response() ──►│
  │               │               │               │
  │               │◄── ④ 完整文本内容 ───────────│
  │               │               │               │
  │◄── ⑤ SSE chunk 1 ───────────│               │
  │  data: {"choices":[{"delta":{"role":"assistant"}}]}   │
  │               │               │               │
  │◄── ⑥ SSE chunk 2 ───────────│               │
  │  data: {"choices":[{"delta":{"content":"这是"}}]}      │
  │               │               │               │
  │◄── ⑦ SSE chunk N ───────────│               │
  │  data: {"choices":[{"delta":{"content":"橘猫"}}]}      │
  │               │               │               │
  │◄── ⑧ SSE [DONE] ────────────│               │
  │               │               │               │
```

### 4.4 级联编排（前处理 + 生成 + 超分，v1.1）

```
Client        Gateway      execute_staged   Script      Upstream_A    Upstream_SR
  │             │             │             │             │             │
  │─ POST /v1/images ────────►│             │             │             │
  │  X-Stages: generate,upscale             │             │             │
  │  X-Stage-Urls: generate=...,upscale=... │             │             │
  │             │             │             │             │             │
  │             │─ ① 脚本编译，读 STAGES ──►│             │             │
  │             │─ ② Budget(total=300s) ───►│             │             │
  │             │             │             │             │             │
  │             │             │─ ③ transform(phase="request:generate") ─►│
  │             │             │   前处理在此完成（ctx.image.resize）      │
  │             │             │◄── ④ 上游 A 请求体 + ctx.emit(url) ──────│
  │             │             │             │             │             │
  │             │             │─ ⑤ _do_upstream，超时=min(60s, 剩余) ───►│
  │             │             │◄── ⑥ image_url ─────────────────────────│
  │             │             │             │             │             │
  │             │             │─ ⑦ transform(phase="response:generate") ►│
  │             │             │◄── ⑧ 产物 → ctx.stage["generate"] ───────│
  │             │             │             │             │             │
  │             │             │─ ⑨ transform(phase="request:upscale") ──►│
  │             │             │   读 ctx.stage["generate"]["image"]      │
  │             │             │─ ⑩ _do_upstream ───────────────────────────────►│
  │             │             │◄── ⑪ 2x 放大结果 ──────────────────────────────│
  │             │             │      ✗ 失败且在 STAGE_FALLBACK → 用 ⑧ 产物降级   │
  │             │             │             │             │             │
  │             │             │─ ⑫ transform(phase="response:upscale") ─►│
  │             │◄── ⑬ 最终 OpenAI 响应 ────│             │             │
  │             │             │             │             │             │
  │◄── ⑭ 200（降级时带 X-Adapter-Degraded 头）│           │             │
  │             │             │             │             │             │
```

要点：每级复用既有的 `_call_phase` / `_do_upstream` / `_run_poll_loop`，级联只是外层循环；  
任一级都可以是 job 型异步上游；`upscale` 失败走降级而非整单失败。

---

## 5. 可观测架构（Logfire）

### 5.1 埋点设计

```python
# adapter/logfire_setup.py
import logfire

def init_logfire(app, settings):
    logfire.configure(
        token=settings.logfire_token,
        service_name="openai-adapter",
        service_version="1.0.0",
        environment=settings.environment,
    )

    # 自动埋点：FastAPI 所有请求
    logfire.instrument_fastapi(app)

    # 自定义全局属性
    logfire.configure_global_tags(
        deployment="docker-compose",
        region=settings.region,
    )
```

### 5.2 Trace 结构

每个请求生成一个 Trace，包含以下 Span：

```
Trace: adapter_request (request_id=uuid)
├── Span: registry_resolve
│   └── Attribute: resolved_upstream, resolved_endpoint, model
├── Span: script_load
│   └── Attribute: script_path, cache_hit, load_duration_ms
├── Span: script_transform_request
│   └── Attribute: script, transform_duration_ms
├── Span: auth_build (optional)
│   └── Attribute: auth_type
├── Span: upstream_http_call
│   └── Attribute: url, method, status_code, latency_ms, request_size_bytes, response_size_bytes
├── Span: async_job_poll (optional, async upstream)
│   ├── Span: poll_attempt_1
│   ├── Span: poll_attempt_2
│   └── Attribute: job_id, poll_count, total_wait_ms
├── Span: image_process (optional, images endpoint)
│   ├── Span: download_image
│   │   └── Attribute: url, size_bytes, cache_hit
│   ├── Span: encode_b64
│   └── Attribute: operation, format
├── Span: script_transform_response
│   └── Attribute: script, transform_duration_ms, output_format
└── Span: response_serialize
    └── Attribute: response_size_bytes, stream
```

级联请求的 Trace 结构（每级一个子 Span，天然可定位「哪一级慢/哪一级降级」；Span 名与实际代码 `logfire.span("adapt")` / `("upstream_call")` 保持一致）：

```
Trace: adapt (endpoint, script_sha256, upstream_url, is_async)
├── Attribute: stages=[generate,upscale], budget_s=300
├── Span: stage (name=generate, remaining_s)
│   ├── Span: script_phase (phase=request:generate)
│   ├── Span: upstream_call (method, url, status)
│   └── Span: script_phase (phase=response:generate)
├── Span: stage (name=upscale, remaining_s)
│   ├── Span: script_phase (phase=request:upscale)
│   ├── Span: upstream_call
│   └── Attribute: degraded=true, degraded_reason  (降级时)
└── Attribute: stage_upstream_calls=2
```

未声明 `STAGES` 的请求，Trace 结构与现状完全一致（`adapt` → `upstream_call`），无新增层级。

### 5.3 关键指标（Metrics）

| Metric                                   | Type      | Labels                     | 说明                       |
| ---------------------------------------- | --------- | -------------------------- | ------------------------ |
| `adapter_requests_total`                 | Counter   | endpoint, upstream, status | 请求总量                     |
| `adapter_request_duration_seconds`       | Histogram | endpoint, upstream         | 端到端延迟                    |
| `adapter_script_duration_seconds`        | Histogram | script, phase              | 脚本执行耗时                   |
| `adapter_upstream_duration_seconds`      | Histogram | upstream, status           | 上游调用耗时                   |
| `adapter_image_process_duration_seconds` | Histogram | operation                  | 图片处理耗时                   |
| `adapter_async_poll_total`               | Counter   | upstream, status           | 异步轮询次数                   |
| `adapter_script_cache_hit`               | Counter   | script                     | 脚本缓存命中                   |
| `adapter_active_requests`                | Gauge     | endpoint                   | 当前活跃请求数                  |
| `adapter_stage_duration_seconds`         | Histogram | stage, status              | 单级耗时（定位瓶颈级）              |
| `adapter_stage_upstream_calls_total`     | Counter   | stage                      | 实际上游调用次数（容量核算，BR-015）    |
| `adapter_stage_degraded_total`           | Counter   | stage, reason              | 降级次数（超分失败等）              |
| `adapter_stage_budget_exceeded_total`    | Counter   | stage                      | 总预算耗尽次数                  |
| `adapter_stage_skipped_total`            | Counter   | stage                      | 整级跳过次数（`ctx.SKIP`，AC-31） |
| `adapter_image_op_duration_seconds`      | Histogram | op                         | `ctx.image` 算子耗时         |

### 5.4 日志规范

使用 `structlog` 输出结构化 JSON 日志，Logfire 自动关联：

```python
logger = structlog.get_logger()
logger.info(
    "upstream_request_completed",
    request_id=ctx.request_id,
    upstream=ctx.upstream_name,
    model=ctx.model,
    latency_ms=latency,
    status_code=status,
    response_size=len(body),
)
```

---

## 6. 部署架构

### 6.1 Docker Compose

```yaml
version: "3.8"

networks:
  adapter-net:
    driver: bridge

services:
  adapter:
    build:
      context: .
      dockerfile: Dockerfile
    image: openai-adapter:latest
    container_name: adapter
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./script_store:/app/script_store:ro  # 脚本仓库（X-Script-Ref 根目录）
    environment:
      - LOGFIRE_TOKEN=${LOGFIRE_TOKEN}
      - REDIS_URL=redis://redis:6379/0
      - MINIO_ENDPOINT=minio:9000
      - MINIO_ACCESS_KEY=minioadmin
      - MINIO_SECRET_KEY=minioadmin
      - MINIO_BUCKET=adapter-temp
      - SCRIPT_REF_DIR=/app/script_store
      - SCRIPT_TIMEOUT=30.0
      - STAGE_BUDGET_DEFAULT=300
      - MAX_IMAGE_PIXELS=33177600
      - ENVIRONMENT=production
    depends_on:
      - redis
      - minio
    networks:
      - adapter-net
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 1G
    user: "1000:1000"  # 非 root 运行
    read_only: true     # 只读根文件系统
    tmpfs:
      - /tmp:noexec,nosuid,size=100m

  redis:
    image: redis:7-alpine
    container_name: adapter-redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    networks:
      - adapter-net
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  minio:
    image: minio/minio:latest
    container_name: adapter-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
    volumes:
      - minio_data:/data
    networks:
      - adapter-net
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

volumes:
  redis_data:
  minio_data:
```

### 6.2 生产部署扩展

```
                    ┌─────────────┐
                    │   Nginx     │
                    │  (LB + SSL) │
                    └──────┬──────┘
                           │
            ┌──────────────┼──────────────┐
            │              │              │
            ▼              ▼              ▼
     ┌──────────┐   ┌──────────┐   ┌──────────┐
     │ Adapter  │   │ Adapter  │   │ Adapter  │
     │ Worker 1 │   │ Worker 2 │   │ Worker 3 │
     │ :8080    │   │ :8080    │   │ :8080    │
     └────┬─────┘   └────┬─────┘   └────┬─────┘
          │              │              │
          └──────────────┼──────────────┘
                         │
                    ┌────┴────┐
                    │  Redis  │  (共享状态)
                    │ Cluster │
                    └─────────┘
                    ┌─────────┐
                    │  MinIO  │  (共享存储)
                    │ Cluster │
                    └─────────┘
```

**Worker 配置**：

```bash
gunicorn adapter.main:app   -k uvicorn.workers.UvicornWorker   -w 4   --bind 0.0.0.0:8080   --worker-tmp-dir /dev/shm   --access-logfile -   --error-logfile -   --capture-output   --enable-stdio-inheritance
```

---

## 7. 目录结构与模块依赖

```
openai-adapter/
│
├── adapter/                    # 核心包
│   ├── __init__.py
│   ├── main.py                 # Starlette 入口，端点注册
│   ├── config.py               # Pydantic Settings
│   ├── router.py               # 请求路由与调度
│   ├── context.py              # AdapterContext
│   ├── engine.py               # ScriptEngine（加载/执行/缓存）
│   ├── sandbox.py              # AST 安全扫描
│   ├── upstream.py             # aiohttp 客户端封装
│   ├── storage.py              # MinIO 客户端封装
│   ├── registry.py             # 上游注册表（YAML）
│   ├── executor.py             # 阶段调度（_call_phase/_do_upstream/execute_staged）
│   ├── async_state.py          # 异步状态机
│   ├── logfire_setup.py        # Logfire 初始化
│   ├── middleware/             # 中间件
│   │   ├── __init__.py
│   │   ├── auth.py
│   │   ├── rate_limit.py
│   │   ├── logging.py
│   │   └── cors.py
│   └── utils/                  # 工具函数
│       ├── __init__.py
│       ├── image.py            # Pillow 封装（ctx.image 的实现，v1.1）
│       └── http.py             # HTTP 辅助
│
├── script_store/               # 脚本仓库（X-Script-Ref 根目录，挂载为只读卷）
│   └── volcengine_ark/
│       └── images@v1.py
│
├── tests/                      # 测试
│   ├── unit/
│   ├── integration/
│   └── scripts/                # 脚本单元测试
│
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── pyproject.toml
└── README.md
```

**模块依赖关系**：

```
main.py
├── router.py
│   ├── registry.py
│   ├── executor.py                  # 阶段调度；级联为其外层循环
│   │   └── engine.py                # 每级复用同一脚本引擎
│   ├── engine.py
│   │   ├── sandbox.py
│   │   └── context.py
│   │       ├── upstream.py (aiohttp)
│   │       ├── storage.py (minio)
│   │       ├── utils/image.py (Pillow → ctx.image)
│   │       └── cache (aioredis)
│   ├── async_state.py
│   └── logfire_setup.py
└── middleware/
    ├── auth.py
    ├── rate_limit.py
    └── logging.py
```

依赖方向仍严格向下（`pipeline` → `engine` → `context` → infra），无反向依赖、无环。

---

## 8. 接口契约

### 8.1 脚本标准接口

> 以实际代码为准（`adapter/script_cache.py::TRANSFORM_NAME`）：脚本只有**一个入口函数**，  
> 用 `phase` 参数区分职责，而非多个具名钩子。

```python
# 唯一必需入口。phase ∈ {auth, request, response, poll_request, poll_response}
# 级联时 phase 形如 "request:generate" / "response:upscale"（§3.8.2）
async def transform(ctx, payload, phase): ...

# 可选模块级声明
PHASES = ["request", "response"]          # 需处理默认之外的阶段时显式声明
STAGES = ["generate", "upscale"]          # v1.1：声明级联，引擎按序驱动
STAGE_FALLBACK = ["upscale"]              # v1.1：允许降级的级
```

`PHASES` 的既有校验规则不变：必须包含 `request`/`response`，未知阶段名报 `SecurityError`。  
`STAGES` 缺省时 phase 名不带后缀，行为与现状完全一致（AC-25）。

### 8.3 级联脚本示例（生图 + 超分）

```python
# script_store/vendor_a/images_hd@v1.py
STAGES = ["generate", "upscale"]
STAGE_FALLBACK = ["upscale"]              # 超分失败则返回原图

async def transform(ctx, payload, phase):
    # 前处理：入图归一化，纯本地计算，不产生上游调用
    if phase == "request:generate":
        src = payload.get("image")
        if src:
            raw = await ctx.image_bytes(src)
            meta = await ctx.image.info(raw)
            if meta["width"] > 1024 or meta["height"] > 1024:
                raw = await ctx.image.resize(raw, 1024, 1024, keep_ratio=True)
            payload = {**payload, "image": ctx.encode_b64(raw)}
        ctx.emit(url=ctx.stage_urls["generate"])
        return {"prompt": payload["prompt"], "width": 1024, "height": 1024}

    if phase == "response:generate":
        return {"image": payload["image_url"]}

    # 后处理：超分。读上一级产物
    if phase == "request:upscale":
        ctx.emit(url=ctx.stage_urls["upscale"], timeout=120)
        return {"image": ctx.stage["generate"]["image"], "scale": 2}

    if phase == "response:upscale":
        return {"created": 0, "data": [{"url": payload["output"]}]}
```

要点：URL 由 `ctx.emit()` 指定、凭证由引擎按 `X-Auth-Emit` 注入，脚本从不接触凭证；  
`ctx.image_bytes()` 已能吃 URL / b64 / data-uri 三种入参，无需脚本自己分支下载。

### 8.2 错误响应格式

```json
{
  "error": {
    "message": "Upstream transform failed: invalid size parameter",
    "type": "invalid_request_error",
    "param": "size",
    "code": "invalid_parameter"
  }
}
```

---

## 9. 非功能性设计

### 9.1 性能

| 指标          | 目标         | 优化手段             |
| ----------- | ---------- | ---------------- |
| 单 Worker 并发 | 1000+ 等待连接 | asyncio 单线程事件循环  |
| 多 Worker 并发 | 4000+ 等待连接 | gunicorn 4 进程    |
| 脚本加载延迟      | < 100ms    | 模块级缓存 + 预加载      |
| 脚本执行延迟      | < 50ms     | 同进程函数调用，零序列化     |
| 图片下载延迟      | < 500ms    | Redis 缓存 5 分钟    |
| 端到端延迟（不含上游） | < 100ms    | 轻量 Gateway，无冗余处理 |

### 9.2 可用性

| 策略    | 实现                                                          |
| ----- | ----------------------------------------------------------- |
| 无状态设计 | 状态外置 Redis，Worker 可随时重启/扩容                                  |
| 健康检查  | `/health` 端点，返回 200 + 依赖状态                                  |
| 优雅关闭  | 收到 SIGTERM 后停止接受新请求，等待活跃请求完成                                |
| 超时熔断  | 分层预算（§3.3.1）：转换钩子 30s、单步 120s、编排总预算 300s、上游调用 60s、异步轮询 120s |
| 重试策略  | 上游 5xx 时指数退避重试 2 次（幂等请求）；级联中每级独立重试，重试耗时一并扣减总预算              |
| 级联降级  | `STAGE_FALLBACK` 声明的后处理级失败时返回上一级产物，标记 `degraded` 而非整单失败     |

### 9.3 安全

| 层级  | 措施                                                                     |
| --- | ---------------------------------------------------------------------- |
| 传输层 | TLS 1.3（Nginx 或 LB 终止）                                                 |
| 认证层 | 内部 Token 校验（与 New API 对接）                                              |
| 脚本层 | AST 扫描 + Import 白名单 + 分层超时熔断（Pillow 等能力经 `ctx` 门面暴露，不放宽 import 白名单）    |
| 级联层 | 各级 URL 只能取自 `X-Stage-Urls`（已过 SSRF 校验），凭证由引擎按 `X-Auth-Emit` 注入，不进脚本可见域 |
| 容器层 | 非 root、只读根文件系统、tmpfs、资源限制                                              |
| 网络层 | 容器间网络隔离，仅 Gateway 暴露端口                                                 |

---

## 10. 演进路线

| 阶段        | 技术目标                                                            | 时间    |
| --------- | --------------------------------------------------------------- | ----- |
| **MVP**   | 支持 `/v1/images` 1-2 个异构上游，基础脚本引擎，Logfire 基础埋点                   | 1 周   |
| **v0.2**  | 支持 `/v1/chat` Vision 输入，脚本热重载，AST 安全扫描                          | 1 周   |
| **v0.3**  | 支持 `/v1/responses` 工具模拟，异步状态机，Redis 状态持久化                       | 2 周   |
| **v0.4**  | 完善可观测（自定义 Metrics + Dashboard），自动化脚本测试框架                        | 1 周   |
| **v1.0**  | 生产就绪：多 Worker、Nginx LB、性能调优、安全加固、文档完善                           | 2 周   |
| **v1.1**  | 级联编排：`STAGES` 声明 + 阶段名后缀、`ctx.image`/`ctx.stage`、总预算、级降级与级 Span | 1.5 周 |
| **v1.2+** | 支持更多上游、流式优化、缓存策略优化、K8s Helm Chart                               | 持续    |

---

*文档版本: v1.1*  
*日期: 2026-09-09*  
*v1.1 变更：新增 §3.3.1 分层超时模型、§3.5.1 ctx 级联增强、§3.8 级联编排、§4.4 级联时序、§5.2 级联 Trace 与 §5.3 级联指标；§8.1 接口契约对齐实际的单入口 `transform(ctx, payload, phase)`*
