# OPEN-DECISIONS 登记册

| Date | Source | Open Item | Related Constraints | Current Leaning | Blocked By | Resolves When | Status |
|------|--------|-----------|---------------------|-----------------|------------|---------------|--------|
| 2026-09-04 | Phase 1.5 | 真实异构上游接入（vendor 名称/API 文档/凭证） | 脚本映射需真实字段 | 先用 Mock 上游打通全链路 | waiting-on-external-condition：等用户提供上游 API 文档 | 用户提供后照 upstream_a/b 示例编写脚本 | OPEN |
| 2026-09-04 | Phase 1.5 | LOGFIRE_TOKEN 实际值注入 | 用户已确认持有 Token | 代码从环境变量读取，.env 留占位 | waiting-on-external-condition：等用户填入 .env | 用户填入后重启即上报云端 | RESOLVED (Resolution: 用户已提供 `LOGFIRE_TOKEN`，已写入 .env，Logfire 全链路追踪已启用。**凭据值不记入本文件**——本行原先把实际 token 明文写在这里，随公开仓库泄漏，2026-09-11 已脱敏并轮换) |
| 2026-09-04 | Phase 1.5 | 沙箱黑名单范围（移除 Subscript/Await） | 03_技术架构 §3.4 原设计误杀合法脚本 | 已裁决：只禁危险 import/exec/eval/dunder | - | 已在 Spec v1.0 落地 | RESOLVED (Resolution: Spec §11，禁 import 白名单外 + exec/eval/__import__/open/compile + dunder 属性访问) |
| 2026-09-11 | 线上报错定位 | 四个入口如何统一到规范契约 | README:5「以 `/v1/images/generations` 为唯一规范格式、脚本只实现一套契约」；`ctx.endpoint` 存在但无脚本使用 | 用户裁决：**按请求 path 判定，路由做前门，无条件、零配置**；不引入渠道选项开关 | - | 已实施（`docs/09`，`api/frontdoor.py`） | RESOLVED (Resolution: chat / responses 各补入口折叠 + 出口封装；脚本零改动。实测四路径全 200，改前 chat / responses 均 400) |
| 2026-09-12 | 用户裁决 | 批量 I/O 并发化的边界：是否突破「`ctx.gather()` 被刻意排除」 | `docs/07` §14.2 原「不做并发/不做 DAG」；§14.1「一个请求 → 一个结果」 | 用户裁决：**只补一条区分——排除的是并发生成调用，不含并发素材物化；§14.2 结论本身不放宽** | - | 已实施（`docs/07` §14.2.1 + §14.3 两条边界） | RESOLVED (Resolution: §14.2.1 划出 materialization 与 orchestration 的分野，判据是机械三条——①不产生额外上游生成调用（计费口径 `n × len(STAGES)` 不变）②失败语义不变（仍任一失败即整体失败）③并发度有上限。**结论未放宽**：仍不做并发生成调用、不做 DAG、`stages.py` 级联仍严格串行。上限落在新配置 `fanout_concurrency`（默认 3）——它**不是** AC-30 那个给 `n` 次上游调用用的 `concurrency`，两者对象不同不可合并。两条实测约束一并写入：`asyncio.TaskGroup` 扇出会把 `ctx.fail()` 的 400 变成 500（`AdapterError` 被包进 `ExceptionGroup` 后 `except AdapterError` 匹配不到，且脚本侧无法 `except*` 解包），必须用 `gather(..., return_exceptions=True)` 手动重抛；`google/images@v1` 的 `allowance` 是 read-then-write 共享状态，须拆两阶段。**代码实现未开工（方案 P3），配置项亦未实现**) |
| 2026-09-11 | 用户裁决 | 前门的历史：拒绝还是截断（两个门） | `docs/01` §4.1 与 `docs/09` §3 原裁决「400 优于静默丢上下文」；规范契约只有一个 `prompt` / 一个 `image`，历史无处承载 | 用户裁决：**截断到最后一条 `user` 轮**（文本与图都只取最后一条，`assistant` 轮忽略），**两个门一致**；未知 `role` 仍 400 | - | 已实施（`frontdoor.messages_to_canonical` / `input_to_canonical`） | RESOLVED (Resolution: 原「多轮一律 400」改为截断。**代价是接受"静默丢上下文"这一最难点查的失败模式**——原裁决拒绝的理由正是它，现由调用方承担。`docs/01`/`docs/09`/`docs/06` 与验收项已同步修订。chat 门按 `messages` 逐轮切分；responses 门按"轮"切分且**连续裸 part 属同一轮**——按 item 截断会削掉指令只留图（`test_responses_bare_parts_are_one_turn_not_several` 守卫，已用变异验证会报红）) |

| 2026-09-13 | 用户裁决 | `fanout_concurrency` 取什么值、以及「把 script_store 全部改到最优版本 + 开启 FANOUT」 | `settings.py:182` 原值 3；`.env` 原值 3（后被会话改成 5）；`docs/07` §14.2.1 只写了「有上限」没写取值口径 | 用户裁决：**取 5**，并把三个渠道全部纳入扇出 | - | 已实施（`volcengine_ark/images@v8` + `settings.py`/`.env`/`.env.example`/`README`/`docs/05`/`docs/07` 五处同步） | RESOLVED (Resolution: ①**取值口径订正**——原注释称「抬 cap 无收益，`script_timeout` 先到」已被 2026-09-13 实测证伪（延迟受限上游 N=9 时 c=3→9 = 927ms→368ms，2.5x）。真正的约束是上游并发行为 + 内存（`cap × max_asset_bytes`）+ `HTTP_POOL_LIMIT_PER_HOST`（排队计入动作超时）。②**`min(cap, N)` 已内建**，故 `MIN_FANOUT_CONCURRENCY` 在结构上是空旋钮（N=1 恒走串行分支），不引入。③取 5：覆盖常态 2~5 张一拍做完，google 的 10 张分两拍，最坏内存 100MB/worker。⚠️ **前提是延迟受限**；带宽受限时抬并发零收益且更易撞 `image_download_timeout`，正确处方是压单张体积) |
| 2026-09-13 | 用户裁决 | 三个渠道的 `stable` 别名是否随 `latest` 前移 | 既有纪律「`stable` 留旧版做逃生阀」（`docs/07` §14.2.2） | 用户裁决：**一并前移到最优版**（ark → `v8`、openai/google → `v2`） | - | 已实施（`script_store/manifest.json`） | RESOLVED (Resolution: 别名前移，**逃生阀失效**——渠道头指 `@stable` 的会立刻拿到新版本，回滚只能改指具体 `@vN`（各版本文件与摘要全部保留）。代价已写入 `docs/07` §14.2.2 与 `docs/05`，供后续判断是否恢复「stable 留旧版」这一纪律) |
| 2026-09-13 | 用户裁决 | `volcengine_ark` 的「最优版本」是哪一档（该渠道两条特性线从未合并） | `@v5` 是对着 `@v2` 写的 ⇒ `@v3` 的 `ref_*` 与 `@v4` 的默认翻转**不在** `@v5`–`@v7` 线上 | 用户裁决：**v8 = v7 + `ref_*` + `ctx.fanout`**（不承接 `@v4` 的 `data_uri` 默认） | - | 已实施（`script_store/volcengine_ark/images@v8.py`，摘要 `a3268580b1c0…`） | RESOLVED (Resolution: 合并档＝`@v7`（水印默认关、两种重试措辞、默认 `image_ref_mode=url`）+ `@v3` 的 `ref_*` 选项组 + N 张参考图经 `ctx.fanout` 并发物化。**未设任何 `ref_*` 时请求体与 `@v7` 逐字节相同**（差分对钉住），变的只是调用方图源上的瞬时并发。默认仍是 `url` + 重试，`@v4` 的默认翻转刻意不承接) |

| 2026-09-13 | 用户裁决 | 是否保留版本切分（`script_store` 里每个渠道多版本并存） | 既有纪律「并发等可观测行为变更一律新建 `@vN`」「`stable` 留旧版做逃生阀」；上面两条 2026-09-13 记录都以多版本为前提 | 用户裁决：**全部用最优版本，其他全部删掉，文件命名 v1** | - | 已实施（三渠道各一个 `images@v1.py`；`@v2` 及以上删除；manifest 只留 v1） | RESOLVED (Resolution: **版本切分取消**。①三渠道各只保留 `images@v1.py`，内容＝此前的最优版（ark 的 v8、openai/google 的 v2），**代码逐节点等价**（AST 去 docstring 比对为真）。②执行顺序按用户要求「**先提交再硬删**」：`v3`–`v8` 此前是 untracked，先提交 `6d03559` 保留，再删 9 个文件 ⇒ 旧版本只存在于 git 历史。③旧 ref **不留别名**（用户裁决），`volcengine_ark/images@v2` 及以上解析为 None ⇒ **渠道头的 `X-Script-Ref` 必须改成 `@v1`（或 `@latest`/`@stable`），否则部署后即失效**。④代价已记账：**回滚机制消失**（没有旧版可退，只能改代码重发版），且 `manifest.json` 变更需重启才生效。⑤测试同步收敛：删除全部版本边界用例（ark v1–v8 差分、google/openai v1/v2 差分），行为断言全部指向 v1；单测 690→641、集成 213→207，减少的正是被删掉的版本差分用例) |

| 2026-09-13 | 用户要求 | `X-Script-Ref` 以 `@stable` 为准；**找不到版本时回退到 `@stable`**，而不是硬失败 | 同日第 16 行③「旧 ref 不留别名、解析为 None、渠道头必须改指 `@v1`」；`adapter/scriptstore/base.py` 原本对未命中的具体版本直接 `script_not_found` | 用户裁决：**加链级兜底**，且 **`stable` 必须存在** | - | 已实施（`base.py` / `manifest.py` / `__init__.py` + 8 个单测） | RESOLVED (Resolution: ①**兜底落在 `ChainStore.get`（链级）**而非 `DirStore`：链级只在**全链都找不到**该具体版本时才降级，所以「某个 root 真有该版本」时仍按根优先级如实服务，**不破钉版语义**（`test_a_version_that_exists_is_never_substituted` 钉住）。②**两种情形刻意不兜底**：无版本 ref（没有版本可替换，且「无版本」属表头畸形而非退役）、`@stable` 自身（否则重复同一次查询）。③降级时 `logger.warning` 点名请求 ref 与降级目标；`read()` 的 404 文案**同时列出两者**，免得把读者引向错误的层。④**`stable` 必须存在**：启动时对缺少该别名的 manifest 条目打 warning（`_warn_about_missing_stable`），另加单测钉住本仓 manifest 三渠道都定义了它。⑤**变异自证 3/3**：关掉兜底 ⇒ 3 红；兜底优先于真命中 ⇒ 仅「不替换真命中」红；让无版本 ref 也兜底 ⇒ 仅「无版本不兜底」红。⑥**取代第 16 行③的口径**：旧 ref 不再「部署后即失效」，而是降级到 `@stable`) |

汇总：1 未决 + 10 已决
